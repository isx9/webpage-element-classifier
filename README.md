# webpage-element-classifier
Multi-class classification of UI elements (buttons, links, headings, text) cropped from website screenshots, using pretrained CNN embeddings and classical ML classifiers.

Database: 
